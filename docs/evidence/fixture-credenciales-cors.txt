# Hallazgo 4 - credenciales fijas del backend didactico y CORS abierto (NO corregido: riesgo aceptado)
# Rama: week4/security-audit-chocotzi
# Fecha: 2026-09-24T23:14:16Z
# Los valores mostrados son fixtures publicos del curso (no credenciales reales).

$ git grep -n -E "course-valid-token|course-refresh-0" -- course-backend | cut -c1-150
course-backend/README.md:6:- `GET /v1/resources` with `Authorization: Bearer course-valid-token`
course-backend/campusops-self-test.mjs:7:      headers: { Authorization: 'Bearer course-valid-token', 'X-Course-Actor': actor,
course-backend/campusops-self-test.mjs:37:    headers: { Authorization: 'Bearer course-valid-token', 'X-Course-Actor': 'coordinator-1',
course-backend/campusops-self-test.mjs:65:  const malformed = await fetch(`${baseUrl}/v1/geocoding?q=zona`, { headers: { Authorization: 'Bearer course
course-backend/campusops.mjs:30:    return send(response, 200, { actorId: input.actorId, role: actors[input.actorId], accessToken: 'course-valid-token
course-backend/campusops.mjs:33:  if (request.headers.authorization !== 'Bearer course-valid-token' || !Object.hasOwn(actors, actorId)) {
course-backend/self-test.mjs:34:    headers: { Authorization: 'Bearer course-valid-token', 'X-Course-Scenario': 'nullable' },
course-backend/server.mjs:46:    if (request.headers.authorization !== 'Bearer course-valid-token') {
course-backend/server.mjs:58:    if (!input || input.refreshToken !== 'course-refresh-0' || scenario === 'invalid_refresh') {
course-backend/server.mjs:61:    return send(response, 200, { accessToken: 'course-valid-token', refreshToken: 'course-refresh-1', expiresIn: 60 });
[exit=0]

$ git grep -n 'access-control-allow-origin' -- course-backend
course-backend/server.mjs:12:    'access-control-allow-origin': '*',
[exit=0]

$ git grep -n 'COURSE_BACKEND_HOST' -- course-backend
course-backend/server.mjs:5:const host = process.env.COURSE_BACKEND_HOST ?? '127.0.0.1';
[exit=0]

$ git grep -n -i 'fixture' -- course-backend/campusops.mjs docs/CAMPUSOPS_API.md | cut -c1-200
course-backend/campusops.mjs:1:// Public, in-memory teaching fixture. Never deploy as institutional authentication.
course-backend/campusops.mjs:29:    if (!input || !Object.hasOwn(actors, input.actorId)) return send(response, 401, { code: 'unknown_fixture_actor' });
docs/CAMPUSOPS_API.md:9:Los actores públicos de prueba son `reporter-1`, `reporter-2`, `technician-1`, `technician-2` y `coordinator-1`. No son matrículas ni miembros del roster. `POST /v1/session/l
[exit=0]

# Archivos que usan esos valores (servidor, autotests, README del backend y contrato de la API):

$ git grep -l -E 'course-valid-token|course-refresh-0' -- .
course-backend/README.md
course-backend/campusops-self-test.mjs
course-backend/campusops.mjs
course-backend/self-test.mjs
course-backend/server.mjs
docs/CAMPUSOPS_API.md
[exit=0]
