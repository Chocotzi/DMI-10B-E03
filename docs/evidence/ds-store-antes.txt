# Hallazgo 2 - archivos .DS_Store en el repositorio (ANTES de la correccion)
# Rama: week4/security-audit-chocotzi
# Fecha: 2026-09-24T23:00:38Z
# Prueba adicional: se crea un archivo vacio docs/.DS_Store solo para esta captura y se borra al terminar.

$ git ls-files | grep -F .DS_Store
.DS_Store
reports/.DS_Store
[exit=0]

$ git log --oneline --diff-filter=A -- .DS_Store reports/.DS_Store
6295ff7 chore: iniciar CampusOps
[exit=0]

$ git check-ignore -v .DS_Store reports/.DS_Store docs/.DS_Store
[exit=1]

$ git status --short | grep -F .DS_Store
?? docs/.DS_Store
[exit=0]

$ git add --dry-run . | grep -F .DS_Store
add 'docs/.DS_Store'
[exit=0]

$ python -c "import re,subprocess;d=subprocess.check_output(['git','show','HEAD:reports/.DS_Store']);print('nombres de entradas legibles dentro de reports/.DS_Store:',sorted({m.group().decode('utf-16-be') for m in re.finditer(rb'(?:\x00[\x20-\x7e]){3,}',d)}))"
nombres de entradas legibles dentro de reports/.DS_Store: ['-01', 'week-01']
[exit=0]
